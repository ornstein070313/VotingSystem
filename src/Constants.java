import java.math.BigInteger;
import java.util.HashMap;

public class Constants
{
    public static final HashMap<Long, Integer> voterIds = new HashMap<>();
    public static final HashMap<VoteOption, BigInteger> voteCount = new HashMap<>();
    //public static final HashSet<VoteOption> optionIds = new HashSet<>();

}
